export const jwtConstants = {
  secret: 'JWTSecret#@!',
};
export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  POSTS = 'posts',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  POSTS = 'POSTS_SERVICE',
}
